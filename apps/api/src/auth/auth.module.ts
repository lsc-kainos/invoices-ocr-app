import { Module } from '@nestjs/common';

// Esqueleto vazio. F1 popula com NextAuth JwtStrategy, JwtAuthGuard,
// CurrentUser decorator, RolesGuard e endpoint GET /api/v1/me.
@Module({})
export class AuthModule {}
